# MCP Search Hub - Production Environment Configuration

# Server configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO
TRANSPORT=streamable-http

# Caching configuration - longer TTLs for production
CACHE_TTL=3600                   # 1 hour legacy cache TTL
CACHE_MEMORY_TTL=3600            # 1 hour memory cache
CACHE_REDIS_TTL=86400            # 24 hours Redis cache for production
REDIS_URL=redis://redis:6379
REDIS_CACHE_ENABLED=true         # Enable Redis cache in production
CACHE_PREFIX=search:prod:        # Production-specific prefix
CACHE_FINGERPRINT_ENABLED=true
CACHE_CLEAN_INTERVAL=3600        # 1 hour cleanup interval

# Budget - Conservative for production
DEFAULT_BUDGET=0.1

# Provider API Keys (REQUIRED)
FIRECRAWL_API_KEY=fc_api_key_xxxxx
EXA_API_KEY=exa_api_key_xxxxx
PERPLEXITY_API_KEY=pplx-xxxxxx
LINKUP_API_KEY=lp_xxxxxxxx
TAVILY_API_KEY=tvly-xxxxxxxx

# Rate limits - Conservative for production
EXA_REQUESTS_PER_MINUTE=60
EXA_REQUESTS_PER_HOUR=500
EXA_REQUESTS_PER_DAY=5000
EXA_CONCURRENT_REQUESTS=10
EXA_COOLDOWN_PERIOD=10  # Longer cooldown in production

# Budgets - Strict enforcement for production
EXA_DEFAULT_QUERY_BUDGET=0.02
EXA_DAILY_BUDGET=10.00
EXA_MONTHLY_BUDGET=150.00
EXA_ENFORCE_BUDGET=true
EXA_BASE_COST=0.01

# Middleware - Full security for production
MIDDLEWARE_LOGGING_ENABLED=true
MIDDLEWARE_AUTH_ENABLED=true  # Enable auth in production
MIDDLEWARE_RATE_LIMIT_ENABLED=true  # Enable rate limiting in production
MIDDLEWARE_RETRY_ENABLED=true

# Authentication - CHANGE THESE\!
API_KEYS=change-me-1,change-me-2,change-me-3
EOF < /dev/null